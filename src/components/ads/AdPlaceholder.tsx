import { Card } from '@/components/ui/card';

export function AdBannerPlaceholder() {
  return <Card className="text-center text-xs text-slate-500">Espacio futuro para patrocinio discreto (no intrusivo).</Card>;
}

export function AdCardPlaceholder() {
  return <Card className="text-sm text-slate-500">Tarjeta de patrocinio futura para zona de reportes.</Card>;
}
